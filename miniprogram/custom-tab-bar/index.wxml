<van-tabbar active="{{ active }}" bind:change="onChange" active-color="#d9fe8f" inactive-color="#9ca3af">
  <van-tabbar-item
    wx:for="{{ tabBars[role] }}"
    wx:key="url"
    icon="{{ item.icon }}"
  >
    {{ item.text }}
  </van-tabbar-item>
</van-tabbar>
