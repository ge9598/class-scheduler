/* ========================================
   全局样式变量与基础样式
   品牌：EDU ASTRA ACADEMY 未来精英学院
   风格：Apple 简洁风 + 品牌色
   ======================================== */

page {
  /* 品牌色板 */
  --primary-color: #0f1a3c;       /* 深海蓝 - 主色 */
  --primary-light: #f0f2f7;       /* 极浅蓝灰 - hover/选中背景 */
  --accent-color: #d9fe8f;        /* 荧光绿 - CTA/强调 */
  --accent-dark: #b8e050;         /* 荧光绿按下态 */
  --secondary-color: #49498e;     /* 星空紫 - 辅助色 */
  --info-color: #00f3ff;          /* 赛博青 - 链接/高亮 */
  --success-color: #34c759;       /* Apple 绿 */
  --warning-color: #ff9500;       /* Apple 橙 */
  --danger-color: #ff3b30;        /* Apple 红 */

  /* 文字色阶 */
  --text-primary: #1a1a2e;
  --text-secondary: #6b7280;
  --text-hint: #9ca3af;

  /* 背景与边框 */
  --bg-page: #f5f5f7;             /* Apple 灰 */
  --bg-card: #ffffff;
  --border-color: #e5e7eb;

  /* 间距 */
  --spacing-xs: 8rpx;
  --spacing-sm: 16rpx;
  --spacing-md: 24rpx;
  --spacing-lg: 32rpx;
  --spacing-xl: 48rpx;

  /* 字号 */
  --font-sm: 24rpx;
  --font-md: 28rpx;
  --font-lg: 32rpx;
  --font-xl: 36rpx;
  --font-xxl: 44rpx;

  /* 圆角 - Apple 大圆角 */
  --radius-sm: 12rpx;
  --radius-md: 20rpx;
  --radius-lg: 28rpx;

  /* 字体栈 - 近似 Glow Sans */
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', Helvetica,
    Segoe UI, Arial, Roboto, 'PingFang SC', 'miui', 'Hiragino Sans GB',
    'Microsoft Yahei', sans-serif;
  font-size: var(--font-md);
  color: var(--text-primary);
  background-color: var(--bg-page);
  box-sizing: border-box;
}

/* ========================================
   通用工具类
   ======================================== */

.container {
  padding: var(--spacing-md);
}

.card {
  background-color: var(--bg-card);
  border-radius: var(--radius-md);
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-md);
  box-shadow: 0 1rpx 6rpx rgba(0, 0, 0, 0.03);
}

.text-primary { color: var(--text-primary); }
.text-secondary { color: var(--text-secondary); }
.text-hint { color: var(--text-hint); }

.flex-row {
  display: flex;
  flex-direction: row;
  align-items: center;
}

.flex-col {
  display: flex;
  flex-direction: column;
}

.flex-between {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.flex-center {
  display: flex;
  justify-content: center;
  align-items: center;
}

.flex-1 {
  flex: 1;
}

.ellipsis {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.page-title {
  font-size: var(--font-xl);
  font-weight: 600;
  margin-bottom: var(--spacing-md);
  letter-spacing: 1rpx;
}

.section-title {
  font-size: var(--font-lg);
  font-weight: 500;
  margin-bottom: var(--spacing-sm);
}

/* ========================================
   品牌按钮工具类
   ======================================== */

/* 荧光绿强调按钮 */
.btn-accent {
  background-color: var(--accent-color) !important;
  color: var(--primary-color) !important;
  border: none !important;
  font-weight: 600;
  letter-spacing: 1rpx;
}

.btn-accent:active {
  background-color: var(--accent-dark) !important;
}

/* 深海蓝按钮 */
.btn-primary-brand {
  background-color: var(--primary-color) !important;
  color: #ffffff !important;
  border: none !important;
}

/* ========================================
   Vant 组件全局主题覆盖
   ======================================== */

page {
  --button-primary-background-color: var(--accent-color);
  --button-primary-border-color: var(--accent-color);
  --button-primary-color: var(--primary-color);
  --tag-primary-color: var(--secondary-color);
  --checkbox-checked-icon-color: var(--accent-color);
  --switch-on-background-color: var(--accent-color);
  --tab-active-text-color: var(--primary-color);
  --tabs-bottom-bar-color: var(--accent-color);
  --cell-group-inset-border-radius: var(--radius-md);
}
